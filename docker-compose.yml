services:
  bash:
    build: .
    entrypoint: bash
    <<: &default
      image: dannyben/alf
      volumes: 
      - .:/app
      - ./alf:/usr/local/bin/alf

  zsh: &zsh
    <<: *default
    environment:
      PS1: "zsh %/ $$ "
    entrypoint: zsh
