
dir() {
  ls -l "$@"
}

g() {
  if [[ $1 = "s" ]]; then
    shift
    git status "$@"
  elif [[ $1 = "l" ]]; then
    shift
    git log --all --graph --date=relative "$@"
  elif [[ $1 = "p" ]]; then
    shift
    git push "$@"
  else
    git "$@"
  fi
}

gg() {
  g p "$@"
}
