commands_dir: commands
enable_view_markers: always
enable_env_var_names_array: never
enable_deps_array: never
strict: set -eo pipefail

usage_colors:
  caption: bold
  command: green
  arg: bold
  flag: bold
  environment_variable: bold
